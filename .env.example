# Copie este arquivo para .env e preencha com a sua chave.
#   cp .env.example .env
#
# O .env está no .gitignore e NUNCA deve ser versionado.
# A aplicação lê a chave exclusivamente desta variável de ambiente.

ANTHROPIC_API_KEY=sk-ant-api03-cole-sua-chave-aqui
